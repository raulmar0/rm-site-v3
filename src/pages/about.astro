---
import ProfileLayout from "../layouts/profile-layout.astro"

const response = await fetch('https://rmperso-pb.fly.dev/api/collections/users/records?filter=(username = "raulmar")');
const data = await response.json();
const user = data.items[0];

console.log(user.about);

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro</title>
	</head>
	<body>

		<ProfileLayout>
      <div class="about-content" set:html={user.about}></div>
    </ProfileLayout>
	</body>
</html>
