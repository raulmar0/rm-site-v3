<div class="top-bar">
  <input id="color-mode" (click)="toggleDarkTheme()" type="checkbox" class="offscreen" />
  <label for="color-mode" class="switch"></label>
</div>

<script>
  

  
</script>

<style>
  @media (min-width: 480px) {
  .top-bar {
    position: absolute;
    top: 0;
    left: 1%;
  }
  .switch {
    border: 2px solid var(--accent-color);
  }
}

.offscreen {
  position: absolute;
  left: -9999px;
}

.top-bar {
  max-width: 100%;
  height: 64px;
  /* background-color: #fff; */
  display: flex;
  gap: 2rem;
  justify-content: flex-end;
  align-items: center;
  margin: 0 20px;
}

.switch {
  position: relative;
  display: inline-block;
  width: 64px;
  height: 32px;
  background-color: var(--border-color);
  border-radius: 40px;
  cursor: pointer;
}
.switch::after {
  content: '☀️';
  font-size: 0.8rem;
  display: flex;
  justify-content: center;
  align-items: center;
  position: absolute;
  width: 20px;
  height: 20px;
  border-radius: 40px;
  background-color: var(--accent-color);
  left: 7px;
  top: 6px;
  transition: all 0.4s
}

input[type='checkbox']:checked + .switch::after {
  content: '🌙';
  transform: translateX(30px);
  background-color: var(--border-color);
}

input[type='checkbox']:checked + .switch {
  background-color: var(--accent-color);
}

.dropdown {
  cursor: pointer;
  height: 30px;
  border-radius: 5px;
  background-color: var(--border-color);
  color: var(--accent-color);
  font-weight: var(--font-weight-bold);
  font-size: 1.4rem;
  font-family: 'Inter', sans-serif;
  border: none;
  padding: 0 0 0 0.5rem;
  border-right: 0.5rem solid transparent
}

</style>